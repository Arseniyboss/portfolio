import { Metadata } from 'next'
import { Heading } from '@styles/globals'

export const metadata: Metadata = {
  title: 'Skills',
}

const Skills = () => {
  return <Heading>Skills</Heading>
}

export default Skills
